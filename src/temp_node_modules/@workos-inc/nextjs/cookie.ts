import { getEnvVariable } from "./utils";

const cookieName = "wos-session";
const cookieOptions = {
  path: "/",
  httpOnly: true,
  secure: true,
  sameSite: "lax" as const,
};

function getCookiePassword() {
  return getEnvVariable("WORKOS_COOKIE_PASSWORD");
}

export { cookieName, cookieOptions, getCookiePassword };
